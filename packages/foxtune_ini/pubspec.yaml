name: foxtune_ini
description: TunerStudio ECU definition (.ini) parser. Pure Dart, no Flutter dependency.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.6.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
