name: foxtune_protocol
description: "Speeduino serial protocol codec: framing, CRC32, pages, realtime data. Pure Dart."
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  foxtune_ini:
    path: ../foxtune_ini

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
