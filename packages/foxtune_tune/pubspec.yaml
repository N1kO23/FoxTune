name: foxtune_tune
description: Tune state, table/curve math and .msq import/export. Pure Dart.
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  foxtune_ini:
    path: ../foxtune_ini
  xml: ^6.5.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
